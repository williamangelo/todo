[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "2.0.0"
description = "A simple TUI todo application"
requires-python = ">=3.8"

[project.scripts]
todo = "todo_cli:main"

[project.optional-dependencies]
dev = ["pytest"]
